name: Inspect official sources
on:
  push:
    branches: [feature/water-v07]
    paths:
      - scripts/inspect-upstream.mjs
      - .github/workflows/inspect-upstream.yml
  workflow_dispatch:
permissions:
  contents: read
jobs:
  inspect:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - run: node scripts/inspect-upstream.mjs
