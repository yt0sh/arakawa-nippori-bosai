# Vercel Git deployment verification

Date: 2026-09-08

This branch verifies that the existing Vercel project `arakawa-nippori-bosai` builds the v0.6 source from GitHub. It does not change application behavior.

Verification targets: the deployed commit, static HTML/CSS/JS, independent evacuation/shelter/utility/water APIs, and comparison with official source data. A READY build alone does not establish that safety-critical data is correct.

Keep the existing production deployment and Google Sites entrance unchanged until live checks pass. No credentials or private configuration belong in this repository.
