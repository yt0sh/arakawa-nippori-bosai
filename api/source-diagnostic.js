// Temporary, fixed-source diagnostic endpoint. Remove before production.
import {plain} from '../lib/arakawa.mjs';
const SOURCES={shelters:'https://bosai.city.arakawa.tokyo.jp/hinan/hinanjyo-ichiran.html',camera:'https://www.ktr.mlit.go.jp/arage/arage00563.html'};
export default async function(req,res){res.setHeader('Content-Type','application/json; charset=utf-8');res.setHeader('Cache-Control','no-store');const key=req.query?.source||'shelters';if(req.method!=='GET'||!SOURCES[key])return res.status(400).json({error:'Unknown source'});try{const r=await fetch(SOURCES[key],{signal:AbortSignal.timeout(10000)});const h=await r.text();const scripts=[...h.matchAll(/<script\b[^>]*\bsrc\s*=\s*["']([^"']+)/gi)].map(m=>m[1]);const images=[...h.matchAll(/<img\b[^>]*>/gi)].map(m=>m[0]);return res.status(200).json({source:SOURCES[key],status:r.status,length:h.length,scripts,images,html:key==='shelters'?h.slice(0,90000):h.slice(0,25000),retrievedAt:new Date().toISOString()})}catch(e){return res.status(502).json({error:String(e),source:SOURCES[key]})}}
