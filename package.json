{
  "name": "arakawa-nippori-bosai-dashboard",
  "private": true,
  "version": "0.7.0",
  "type": "module",
  "engines": {"node": ">=20"},
  "scripts": {
    "build": "node scripts/build-v07.mjs",
    "test": "npm run build && node --test tests/*.test.mjs",
    "check": "npm run build && node --check dist/app.js && node --check dist/water-v07.js && node --check lib/shelter-feed.mjs && node --check lib/camera.mjs"
  }
}
