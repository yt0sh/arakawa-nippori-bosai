{
  "name": "arakawa-nippori-bosai-dashboard",
  "private": true,
  "version": "0.6.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check public/app.js && node --check lib/arakawa.mjs && node --check lib/water.mjs"
  }
}
